apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: devpulse
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
